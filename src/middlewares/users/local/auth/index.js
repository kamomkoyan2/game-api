const code = require('./code');
const isVerified = require('./isVerified');
const login = require('./login');
const notVerified = require('./notVerified');
const signup = require('./signup');

module.exports = {
    code,
    isVerified,
    login,
    notVerified,
    signup,
}